# dependencies
node_modules/
.node/

# desktop shell — deps, staged Next output, and built installers
desktop/node_modules/
desktop/app/
desktop/dist/
desktop/.prebuild/

# next.js
.next/
out/
next-env.d.ts

# build info
*.tsbuildinfo

# local board data (SQLite, incl. WAL sidecars)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.sqlite
data/*.sqlite3

# secrets — .env.example is committed on purpose
.env
.env.local
.env.*.local

# test / tooling output
coverage/
*.log
npm-debug.log*

# editor & OS
.DS_Store
.idea/
.vscode/

# website — local-only marketing site, uploaded to Netlify directly
landing/
